import React from 'react'
import './Backdrop.css';

const Backdrop = (props) => {
    return (
        <div onClick={props.onClick} className="back-drop">
            
        </div>
    )
}

export default Backdrop
